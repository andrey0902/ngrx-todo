import { DragTaskDirective } from './drag-task.directive';

describe('DragTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DragTaskDirective();
    expect(directive).toBeTruthy();
  });
});
